// In programming, iteration refers to looping through a set of instructions until a condition is met.

// Array Iteration: 

let languages = ['Java', 'Cpp', 'Javascript', 'Php', 'Python']

// Iteration using for loop

for (let i = 0; i < languages.length; i++) {
    const e = languages[i];
    console.log(e); 
}